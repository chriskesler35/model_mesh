# DevForgeAI — Python Dependencies
# Compatible with Python 3.11 – 3.14
# Local install uses SQLite (zero-config).
# PostgreSQL drivers are in requirements.postgres.txt (Docker/production only).

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database (SQLite — default local setup)
sqlalchemy>=2.0.30
aiosqlite>=0.22.1
alembic>=1.13.1

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# AI / LLM
litellm>=1.40.0
tiktoken>=0.7.0
google-generativeai>=0.5.0

# HTTP client
httpx>=0.27.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# Media conversion
Pillow>=10.3.0
pillow-heif>=0.16.0

# Document extraction and export
pypdf>=5.0.0
python-docx>=1.1.2
reportlab>=4.2.0
openpyxl>=3.1.5
python-pptx>=0.6.23

# Auth (multi-user collaboration)
bcrypt>=4.2.0
PyJWT>=2.10.0
cryptography>=42.0.0

# Redis (optional, for caching)
redis>=5.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
