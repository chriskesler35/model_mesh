# PostgreSQL drivers — only needed for Docker / production deployments.
# Install with: pip install -r requirements.postgres.txt
-r requirements.txt
asyncpg==0.29.0
psycopg2-binary==2.9.9
redis==5.0.1
