[project]
name = "modelmesh"
version = "0.1.0"
description = "Intelligent AI gateway"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["app"]
omit = ["app/main.py"]